# Exact versions used to produce the released numbers.
# Several reported values move at the third decimal place across minor versions,
# so these are pinned exactly rather than by lower bound.
numpy==2.0.2
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.6.1
lightgbm==4.6.0
ngboost==0.5.11
xgboost==3.2.0
torch==2.10.0
matplotlib==3.10.0

# Optional: only needed for `python reproduce.py --with-recent`
# pytabkit==1.7.3
